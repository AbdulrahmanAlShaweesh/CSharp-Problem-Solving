namespace Problem2
{
    internal class Program
    {
        static void Main(string[] args)
        {
             /* 2. Write C# program that converts a string to an integer, but the string contains non-numeric characters.
             And mention what will happen */
             Console.WriteLine("Please Enter a value ? ");


            // int Number = Convert.ToInt32(Console.ReadLine());   // will through exception "system.formatexception", due to can not convert the non-numberical into int
            // Console.WriteLine(Number);
        }
    }
}
